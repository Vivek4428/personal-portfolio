document.getElementById('contact-form').addEventListener('submit', function(e) {
    e.preventDefault();
    alert('Form submitted successfully!');
    // Here you can add code to handle the form submission, like sending the data to an email or server.
});
